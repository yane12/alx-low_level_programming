#include "main.h"
#include <stdlib.h>

char *string_nconcat(char *s1, char *s2, unsigned int n)
{
    char *str;
    int count = 0, i;
  
    for(i=0; s1[i] || s2[i]; i++)
        count++;

    for(i=0; s1[i]; i++)
    {
      s[i] = s1[i];
    }
    
}
